#include <bits/stdc++.h>
using namespace std;
#define Arep(i, n, m) for(int i = n; i < (int)(m); i++)
#define Drep(i, n, m) for(int i = m-1; i >= (int)(n); i--)
#define all(v) v.begin(), v.end()

int main() {
    /* input */
    int64_t N;
    cin >> N;

    /* solve */
    N--; // 0は回文数じゃない、1が最小の回文数ということにする
}