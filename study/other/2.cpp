#include <bits/stdc++.h>
#include <atcoder/dsu>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(v) v.begin(), v.end()

int main() {
    vector<int> x;

    x.push_back(1);

    rep(i, x.size()) cout << x.at(i) << " ";
}