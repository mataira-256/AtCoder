#include <bits/stdc++.h>
#include <atcoder/dsu>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(v) v.begin(), v.end()

int main() {
    int A, B;

    cin >> A >> B;

    cout << "A:";
    rep(i, A) cout << "]";
    cout << endl; 

    cout << "B:";
    rep(i, B) cout << "]";
    cout << endl;
}